<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chaos.library.mapper.UserMapper">

    <select id="findByUsername" resultType="com.chaos.library.entity.User">
        SELECT * FROM `user` WHERE username = #{username}
    </select>

    <select id="findById" resultType="com.chaos.library.entity.User">
        SELECT * FROM `user` WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, role, name, phone, created_at, updated_at)
        VALUES (#{username}, #{password}, #{role}, #{name}, #{phone}, NOW(), NOW())
    </insert>

    <update id="update">
        UPDATE `user`
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="name != null">name = #{name},</if>
            <if test="phone != null">phone = #{phone},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

</mapper>